package integratedtoolkit.types.annotations.parameter;

/**
 * Stream types
 */
public enum Stream {
    STDIN, // standard in
    STDOUT, // standard out
    STDERR, // standard error
    UNSPECIFIED; // no stream
}