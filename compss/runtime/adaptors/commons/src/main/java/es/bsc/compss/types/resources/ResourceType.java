package es.bsc.compss.types.resources;

/**
 * Types of registrable resources.
 */
public enum ResourceType {
    MASTER, // For the master node
    WORKER, // For the worker nodes
    SERVICE // For services
}
