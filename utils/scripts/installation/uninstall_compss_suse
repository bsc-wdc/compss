#!/bin/bash -e

  # Remove compss
  zypper --non-interactive remove -y --clean-deps compss-*

  # Purge compss

  # Clean all

  # Update DB
  zypper refresh

  # Exit with status from last command
  exit

