compss pipes

pycompss -> binding commons -> RUNTIME --(pipe out)-> python worker

pythohn worker -> bindings commons -> --(pipe in)-> Runtime -> nova tasca (amb colections)

al binding commons a compss_worker/compss_jni/ tenen la seva equivalencia amb compssruntimeimpl

process_params de compss_pipes crea el string del que s'ha d'executar que acaba entrant per compssRuntimeImple (execute task o així). A dintre de process params és on va afegint al comando que cridarà amb la eecució per la tasca

executenestedpiptaskcomand tiene el metodo del ejecutor que lee el mensaje de la pipe que enviar el worker con los datos de lo que hay que ejecutar i acaba llamando a executetask del runtime

pipedinvoker.java linea ~100 está entrando por ahí y hace el parseo del tipo de mensaje o algo asi(?)
