{
    "execution" : {
        "type":"task"
    },
    "container": {
        "engine": "DOCKER",
        "image": "${COMPSS_IMAGE}",
	"options": "${DOCKER_OPTIONS}"
    },
    "parameters": {
        "returns": 1
    }
}
